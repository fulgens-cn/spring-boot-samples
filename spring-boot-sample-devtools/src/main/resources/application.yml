spring:
  application:
    name: spring-boot-sample-devtools
  thymeleaf:
    encoding: UTF-8
    mode: HTML
    prefix: classpath:/templates/
    suffix: .html
    servlet:
      content-type: text/html
  devtools:
    restart:
      log-condition-evaluation-delta: true
      enabled: true